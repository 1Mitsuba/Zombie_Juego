// Minimal stub to neutralize previous placeholder components.
// Replaced with simple exports so any accidental imports won't crash.
export const Bg = ({ children }) => children || null;
export const Heart = () => null;
export const PowerIcon = () => null;

export default null;
